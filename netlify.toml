[build]
  publish = "."
  command = "node scripts/netlify-config.mjs"

# SPA: el mapa vive en index.html en la raíz del repo
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

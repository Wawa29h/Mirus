[build]
  publish = "."
  command = "node scripts/netlify-config.mjs"

# El mapa real también accesible en /frontend/ (UI modo ruta + iframe)
# Raíz / sigue siendo el mapa a pantalla completa
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

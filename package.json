{
  "name": "mirus",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "serve": "npx serve -l 3000 . --config serve.json",
    "build": "node scripts/netlify-config.mjs"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
